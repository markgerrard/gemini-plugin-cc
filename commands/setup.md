---
description: Check Gemini CLI availability and show available commands
disable-model-invocation: true
allowed-tools: Bash(node:*)
---

Check if Gemini CLI is installed and authenticated.

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/gemini-companion.mjs" setup
```
